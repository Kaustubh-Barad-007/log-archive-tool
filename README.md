# Log Archive Tool

A simple shell script to compress log files into a `.tar.gz` archive with date and time.

## How to Run in Windows (VS Code Terminal / PowerShell)

Run this command directly in your terminal:

```bash
wsl sh log-archive-tool.sh /var/log
